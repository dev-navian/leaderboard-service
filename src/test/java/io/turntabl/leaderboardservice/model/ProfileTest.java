package io.turntabl.leaderboardservice.model;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;

import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

class ProfileTest {
//    @Mock
//    private Profile profile;
//
//    @Test
//    void setLanguageLevels() {
//        LanguageLevel languageLevelOne = mock(LanguageLevel.class);
//        LanguageLevel languageLevelTwo = mock(LanguageLevel.class);
//
//        List<LanguageLevel> languageLevels = List.of(languageLevelOne, languageLevelTwo);
//
//        when(profile.setLanguageLevels(languageLevels)).thenReturn(profile);
//
//        List<LanguageLevel> expected = profile.getLanguageLevels();
//
//        assertThat(expected).containsExactly(languageLevelOne, languageLevelTwo);
//
//    }
}
